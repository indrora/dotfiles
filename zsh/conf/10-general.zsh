# 10-general.zsh 
# Generalized configuration stuff.
# In this case, history and save size.
# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=100
SAVEHIST=100
setopt appendhistory notify
bindkey -e
